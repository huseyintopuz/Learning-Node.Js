import os from "os";

// userInfo()
console.log(os.userInfo());

// totalmem() it gives total memory in bytes
console.log(os.totalmem());

// freemem() it gives free memory in bytes
console.log(os.freemem());

// cpus()
console.log(os.cpus());